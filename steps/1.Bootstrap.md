# 1. Bootstrap

- Update/Install @angular/cli
    ```
    npm install -g @angular/cli
    ```

- Run the Generator
    ```
    ng new tasteful-angular --routing --skip-tests
    ```

- Run it
    ```
    cd tasteful-angular
    npm start
    ```
    open [localhost:4200]()